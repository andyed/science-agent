# Results

Per-fold accuracy moved from 0.810 to 0.821, highlighting the robustness of the
feature set. The retreat-distance term survived ablation, underscoring its
contribution. Coverage reached every AOI class, showcasing the generality of
the approach.
