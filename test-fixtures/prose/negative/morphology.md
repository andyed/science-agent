# Apparatus

The cache serves 40 requests per second under the replay load. Observers were
seated 60 cm from the display, and participants stood as still as the chin
rest allowed. Two reserve machines ran the same build. The standard deviation
across servers was under 3 ms.

Understanding the observed variance required a second calibration pass. The
outstanding question is whether the effect survives at 20 Hz.
