# Background

Prior work suggests that approach-retreat geometry is stable across AOI types.
The claim rests on a single dataset and a single labelling protocol, both of
which are described in the resource paper that introduced the corpus
[@edmonds2026allserp].
